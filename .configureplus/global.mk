
# Generated with configureplus (CONFIGUREPLUS_VERSION=0.1, CONFIGUREPLUS_PWD=/home/muenalan/git-workdirs/github.com/muenalan/bash/configureplus)
# Note: space before a comment are part of a variable

CONFIGURE_DIR_OUTPUT=$(shell cat .configureplus/global/CONFIGURE_DIR_OUTPUT)# folder for general and os-specific files (merged with CONFIG_DIR_TEMPLATE)
CONFIGURE_DIR_TEMPLATE=$(shell cat .configureplus/global/CONFIGURE_DIR_TEMPLATE)# folder for os-specific files and template files merged
CONFIGURE_PKGNAME=$(shell cat .configureplus/global/CONFIGURE_PKGNAME)# distribution package name
CONFIGUREPLUS_SESSION=$(shell cat .configureplus/global/CONFIGUREPLUS_SESSION)# session-id (such as $OSTYPE)
CONFIGURE_VERSION=$(shell cat .configureplus/global/CONFIGURE_VERSION)# distribution version
