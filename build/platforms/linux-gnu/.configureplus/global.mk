
# Generated with configureplus (CONFIGUREPLUS_VERSION=0.14.5, CONFIGUREPLUS_PWD=/home/muenalan/git-workdirs/configureplus/build/platforms/linux-gnu)
# Note: space before a comment are part of a variable

CONFIGUREPLUS_SESSION=$(shell cat .configureplus/global/CONFIGUREPLUS_SESSION)
CONFIGURE_DIR_OUTPUT=$(shell cat .configureplus/global/CONFIGURE_DIR_OUTPUT)
CONFIGURE_DIR_TEMPLATE=$(shell cat .configureplus/global/CONFIGURE_DIR_TEMPLATE)
CONFIGURE_GIT_TAG=$(shell cat .configureplus/global/CONFIGURE_GIT_TAG)
CONFIGURE_PKGNAME=$(shell cat .configureplus/global/CONFIGURE_PKGNAME)
CONFIGURE_VERSION=$(shell cat .configureplus/global/CONFIGURE_VERSION)
