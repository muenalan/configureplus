
# Generated with configureplus (CONFIGUREPLUS_VERSION=0.16.0, CONFIGUREPLUS_PWD=/Users/muenalan/git-workdirs/github.com/muenalan/bash/configureplus/build)
# Note: space before a comment are part of a variable

CONFIGURE_DIR_OUTPUT=$(shell cat .configureplus/global/CONFIGURE/DIR_OUTPUT)# folder for general and os-specific files (merged with CONFIG_DIR_TEMPLATE)
CONFIGURE_GIT_TAG=$(shell cat .configureplus/global/CONFIGURE/GIT_TAG)
CONFIGURE_VERSION=$(shell cat .configureplus/global/CONFIGURE/VERSION)# distribution version
CONFIGURE_DIR_TEMPLATE=$(shell cat .configureplus/global/CONFIGURE/DIR_TEMPLATE)# folder for os-specific files and template files merged
CONFIGURE_PKGNAME=$(shell cat .configureplus/global/CONFIGURE/PKGNAME)# distribution package name
CONFIGUREPLUS_SESSION=$(shell cat .configureplus/global/CONFIGUREPLUS/SESSION)# session-id (such as $OSTYPE)
CONFIGUREPLUS_SESSION=$(shell cat .configureplus/global/CONFIGUREPLUS_SESSION)
