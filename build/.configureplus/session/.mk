
# Generated with configureplus (CONFIGUREPLUS_VERSION=0.1, CONFIGUREPLUS_PWD=/Users/muenalan/git-workdirs/github.com/muenalan/bash/configureplus/build)
# Note: space before a comment are part of a variable

CONFIGUREPLUS_DIR_CONFIG=$(shell cat .configureplus/session//CONFIGUREPLUS_DIR_CONFIG)
CONFIGUREPLUS_DIR_OUT=$(shell cat .configureplus/session//CONFIGUREPLUS_DIR_OUT)
CONFIGUREPLUS_DIR_OUT_SESSIONS=$(shell cat .configureplus/session//CONFIGUREPLUS_DIR_OUT_SESSIONS)
CONFIGUREPLUS_PWD=$(shell cat .configureplus/session//CONFIGUREPLUS_PWD)
CONFIGUREPLUS_SESSION=$(shell cat .configureplus/session//CONFIGUREPLUS_SESSION)# session-id (such as $OSTYPE)
CONFIGUREPLUS_VERSION=$(shell cat .configureplus/session//CONFIGUREPLUS_VERSION)
CONFIGURE_BASH_PROFILE_FILE=$(shell cat .configureplus/session//CONFIGURE_BASH_PROFILE_FILE)
CONFIGURE_DIR_OUTPUT=$(shell cat .configureplus/session//CONFIGURE_DIR_OUTPUT)# folder for general and os-specific files (merged with CONFIG_DIR_TEMPLATE)
CONFIGURE_DIR_TEMPLATE=$(shell cat .configureplus/session//CONFIGURE_DIR_TEMPLATE)# folder for os-specific files and template files merged
CONFIGURE_FLAG_TOOL_BTEST=$(shell cat .configureplus/session//CONFIGURE_FLAG_TOOL_BTEST)# btest *testing* tool path
CONFIGURE_GIT_TAG=$(shell cat .configureplus/session//CONFIGURE_GIT_TAG)
CONFIGURE_MKTEMP=$(shell cat .configureplus/session//CONFIGURE_MKTEMP)# make session temp dir
CONFIGURE_OSTYPE=$(shell cat .configureplus/session//CONFIGURE_OSTYPE)# current os identifier
CONFIGURE_PKGNAME=$(shell cat .configureplus/session//CONFIGURE_PKGNAME)# distribution package name
CONFIGURE_TIMESTAMP=$(shell cat .configureplus/session//CONFIGURE_TIMESTAMP)
CONFIGURE_VERSION=$(shell cat .configureplus/session//CONFIGURE_VERSION)# distribution version
